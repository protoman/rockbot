
- #2: usar mais tiles animados (lava, cascatas, neve, areia)


- traduzir strings novas que estao só em ingles
    # dar uma re-testada após as últimas mudanças


- usar configuração do telefone/java para definir o idioma a ser usado (só para pt-BR)

- opção para usar músicas antigas
    # tem que criar algo na config (já deixar uns tempos a mais sobrando lá, para o futuro)
    # usar mesmos nomes, só colocar em uma pasta old
    # todos devem ter extensão .mod
    # separar músicas ogg em pasta, para não aparecerem no editor


- Rockbot1:
    # Entrou cutscene intro antes dos créditos ???????

- Rockbot2:
    # Kurupura: shooter teto não para direita até muro. Colocar checkpoint ao voltar para parte aberta. AI do Kurupira está ridícula.
    # Haunted mansion música demora tocar e não tinha trocado ela?
    # Projétil da escavadeira se perde com scroll.
    # Escudo da escavadeira está impenetrável pela esquerda.
    # Snow ficou preso na porta na fase do castelo (dash/girando)
    # pegar músicas do JMD

- esta travando ainda no Dingoo e PocketGo, pode ser algum loop que fica infinito
    # game.cpp - criar lista de projéteis, objetos e inimigos on-screen e armazenar em shared (singleton)
    # todos os loops que passam por projéteis, inimigos e objetos devem passar a usar essas listas


- PocketGo Port
    # audio delay

- novos inimigos (https://www.youtube.com/watch?v=U8fQ-k2p5Q8&list=WL&index=2&t=514s):
    # jumper (from hole) that fires 4 shots in arc
    # marreta: bata-estacas vindo do teto (não letal ao contrário dos raios da morte)

- parece que o tank de HP não esta funcionando

- recreate intro (use new drawings, create a different story)

- (Rafaela Coiro) não dei nota máxima por causa do bug na penúltima fase que quando morre pro chefe eu revivo dentro do teto

- checkpoint fica parado no ar em restart_stage, bloqueado pelo jogador
    # somente plataformas devem colidir com jogador

- inimigos param de atirar após um tempo (? feedback de usuário, não consegui entender)

- hit-area de objetos tipo teleportadores não esta funcionando

- dizem que seleciona fase mas vai para a mesma (testar melhor)

- bug: textos fora de posição (tem que ter tamanho virtual de fonte para cálculo de posição)
    # seleção de player
    # config do menu de armas

- [2] final boss teleporter não está funcionando (sem animação, não teleporta)

- criar intro em texto (soldado e blá blá blá)
    # aparece apenas ao selecionar new game

- ghosts devem aparecer abaixo do cenário


##########################################
BACKPORT FIREFLY:
##########################################
- Config integer-value method
- revisar lista de fixes



8cm largura embaixo
9cm largura em cima
3cm altura
